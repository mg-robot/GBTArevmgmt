<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Global_Partner__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Internal lookup to the Global Partner (Account). Populated by staff/automation; activation still required.</description>
    <inlineHelpText>Internal lookup to the Global Partner (Account). Populated by staff/automation; activation still required.</inlineHelpText>
    <label>Global Partner</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Global Partner Assets</relationshipLabel>
    <relationshipName>Assets</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
